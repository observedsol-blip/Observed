/**
 * SealMoment — deliberately empty placeholder.
 *
 * The concept decision is OPEN. 03-SCREEN-MAP.md names exactly one motion in
 * the whole app, and it is the strike on Result ("pending" struck through,
 * "observed" set above it in pencil) — not a seal moment. Whether a moment at
 * the seal should exist at all, and whether it would be motion or a static
 * mark, has not been decided by the owner.
 *
 * Until that decision is made this renders nothing. The strike on Result is
 * rendered statically, with no animation, so that nothing here pre-empts it.
 */
export default function SealMoment(): null {
  return null;
}
