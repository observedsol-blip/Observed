# Spikes — facts, not estimates

One file per spike: `pyth.md`, `sgt.md`, `seeker.md`. Each records: what was run, exact numbers (tx count, bytes, compute units, fees, rent, wallet approvals), which rejection cases passed/failed, and the one sentence that changes the spec (if any).
