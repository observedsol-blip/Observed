target/
node_modules/
.anchor/
app/android/app/build/
*.keystore
keys/
.env
.env.*
test-ledger/
.spec-unlock
.anchor
target
**/*.rs.bk
test-ledger
.surfpool

# Arbeitsspuren fremder Werkzeuge im Projektordner. Codex legt waehrend eines Scans
# .codex/.tmp/ mit Plugin-Caches an (fremde Dokumentation, Beispiel-Keys). Ohne diese
# Zeile faengt ein "git add -A" das mit ein - genau der Weg, auf dem Fremdes in ein
# Repo kommt, das oeffentlich werden soll.
.codex/
.claude/.tmp/
spikes/baserate/*_0416.json
